1> final
	修饰表明为不可变的。仅仅不能和abstract连用。
2> static
	static fields can only be declared in static or top level types.
	static method can only be declared in static or top level types.
	修饰表明为静态的，属于类的，不属于单个对象，所有对象共用的。
3> abstract
	修饰表明为抽象的。
4> public protected default private
	局部变量不能用访问修饰符来修饰。
5> byte char short int long float double
6> do while if else for switch case
7> break continue
8> this super
9> class Interface abstract
10> extends implements
11> import package
12> throw throws
	若想要自行抛出异常，您可以使用“throw”关键词，并生成指定的异常对象。
13> try catch finally
14> void return
	return:如果方法设定为无返回值，可以用"return ;"直接退出方法。
15> instanceof
	例：对象 instanceof 类，如果为对象是该类的实例，则返回true，反之false。
16> native
	在方法前修饰，表示为调用本地方法。
17> transient
	用该关键字修饰的成员，不会被序列化存入。不影响其他正常操作。
18> goto const
	这两个为保留字。
